# pdf-a11y site

A single static HTML file. No build step and no dependencies.

## Preview locally

```bash
npx serve site
```

## Deploy

Vercel: set the project root to `site` and framework preset to "Other".
GitHub Pages: set the source to the `site` folder on the `main` branch.

Fonts load from Google Fonts. Everything else is inline.
